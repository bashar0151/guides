# Dependencies - We install these inside Docker, don't copy local ones
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docusaurus build outputs - We want a fresh build in Docker
build
.docusaurus

# Environment and Secrets - Keep your keys safe
.env
.env.local
.env.production
.env.*.local

# Version Control
.git
.gitignore
.github

# Docker files - The image doesn't need its own instructions
Dockerfile
docker-compose*.yml

# OS Junk
.DS_Store
Thumbs.db